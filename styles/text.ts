import { StyleSheet } from "react-native"

export const textStyles = StyleSheet.create({
    title: {
        fontWeight: "bold",
        fontSize: 20,
    },
    bold: {
        fontWeight: "bold",
    },
    small: {
        fontSize: 12,
    },
})
