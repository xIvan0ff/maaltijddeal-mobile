import { StyleSheet } from "react-native"

export const textStyles = StyleSheet.create({
    title: {
        fontWeight: "bold",
        fontSize: 24,
    },
    bold: {
        fontWeight: "bold",
    },
    small: {
        fontSize: 12,
    },
})
